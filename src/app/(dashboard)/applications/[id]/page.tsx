// TODO (PRD 7.3/7.4): application detail view with its interview rounds
// (add rounds, update round status). Data hooks: src/hooks/useApplications.ts,
// src/hooks/useInterviewRounds.ts
export default function ApplicationDetailPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">Application</h1>
      <p className="mt-1 text-sm text-gray-500">TODO — detail + interview rounds</p>
    </div>
  );
}

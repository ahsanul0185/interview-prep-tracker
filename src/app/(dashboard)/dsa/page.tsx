// TODO (PRD 7.5): DSA topic tracker — Not Started / In Progress / Confident,
// with "confident out of total" summary.
export default function DsaPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">DSA Tracker</h1>
      <p className="mt-1 text-sm text-gray-500">TODO — topic list + progress (PRD §7.5)</p>
    </div>
  );
}

// TODO: dashboard overview —
//   - per-stage application counts (PRD 7.3)
//   - overdue / due-today reminders flagged (PRD 7.7)
//   - DSA confidence progress summary (PRD 7.5)
export default function DashboardPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">Dashboard</h1>
      <p className="mt-1 text-sm text-gray-500">TODO — overview widgets</p>
    </div>
  );
}

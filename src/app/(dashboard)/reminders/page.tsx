// TODO (PRD 7.7): reminders — title, due date, optional linked application,
// done flag; overdue and due-today items visually flagged.
export default function RemindersPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">Reminders</h1>
      <p className="mt-1 text-sm text-gray-500">TODO — reminder list (PRD §7.7)</p>
    </div>
  );
}
